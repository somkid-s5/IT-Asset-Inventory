# คู่มือการรวบรวมข้อมูลสินทรัพย์ไอที (IT Asset Data Collection Guide)

เอกสารฉบับนี้สรุปรายการข้อมูลที่ **จำเป็น (Required)** และ **แนะนำให้มี (Recommended/Optional)** สำหรับการนำเข้าข้อมูลระบบหรือเครื่องเซิร์ฟเวอร์เข้าสู่ระบบ InfraPilot รุ่นใหม่ที่รองรับ Flexible Metadata

---

## 1. ข้อมูลหลักของระบบ (Core System Info)
โครงสร้างหลักที่ใช้ในการระบุตัวตนและจัดกลุ่ม Asset ทุกประเภทในระบบ

| หัวข้อ (Field) | ความจำเป็น | ประเภทข้อมูล | คำอธิบาย & ตัวอย่าง |
| :--- | :---: | :--- | :--- |
| **Asset Name** | 🔴 **บังคับ** | Text | ชื่อระบบ, Hostname, หรือชื่อโปรเจค (เช่น `ESXi-Host-01`, `HR-Portal`) |
| **Asset Type** | 🔴 **บังคับ** | ตัวเลือก (Dropdown) | ประเภทของระบบ: `SERVER` (เครื่องจริง/Network/FW), `VM` (เครื่องเสมือน), `APP` (เว็ปแอปพลิเคชัน), `DB` (ฐานข้อมูล) |
| **Environment** | 🟡 แนะนำ | Text | สภาพแวดล้อม: `PROD` (Production), `UAT` (Testing), `DEV` (Development) |
| **Location** | 🟡 แนะนำ | Text | สถานที่ตั้ง หรือ โซน (เช่น `DC-Bangkok / Rack 04 / U12`, `AWS ap-southeast-1`) |
| **OS Version** | 🟡 แนะนำ | Text | ระบบปฏิบัติการ (เช่น `VMware ESXi 8.0`, `Ubuntu 24.04 LTS`, `Windows Server 2022`) |
| **Status** | 🟢 มีค่าเริ่มต้น | ตัวเลือก (Dropdown) | สถานะปัจจุบัน: `ACTIVE` (ใช้รันเป็นค่าเริ่มต้น), `INACTIVE`, `MAINTENANCE`, `DECOMMISSIONED` |
| **Parent Asset** | 🟡 แนะนำ | Relation | ระบุความสัมพันธ์ (เช่น ถ้าเป็น VM ให้ระบุว่ารันอยู่บน `ESXi-Host-01` ตัวไหน) |

---

## 2. ข้อมูลเครือข่ายและการเชื่อมต่อ (Network & IP Allocation)
อัปเดตใหม่รองรับระบบที่มีหลาย IP ในเครื่องเดียว

| หัวข้อ (Field) | ความจำเป็น | ประเภทข้อมูล | คำอธิบาย & ตัวอย่าง |
| :--- | :---: | :--- | :--- |
| **IP Address** | 🔴 **บังคับ** | Text | ไอพีแอดเดรส (เช่น `192.168.10.50`) **หมายเหตุ: ใส่เพิ่มได้ไม่อั้น** |
| **IP Type** | 🟡 แนะนำ | Text | หน้าที่ของไอพีนี้ (เช่น `Management (iDRAC)`, `VIP`, `Primary Data`, `vMotion`) |

---

## 3. ข้อมูลบัญชีผู้ใช้และรหัสผ่าน (Credentials & Vault)
ระบบจะทำการเข้ารหัสระดับสูง (AES-256-GCM) ทันทีที่บันทึกข้อมูลเหล่านี้

| หัวข้อ (Field) | ความจำเป็น | ประเภทข้อมูล | คำอธิบาย & ตัวอย่าง |
| :--- | :---: | :--- | :--- |
| **Username** | 🔴 **บังคับ** | Text | ชื่อบัญชีล็อกอิน (เช่น `root`, `admin`, `postgres_sys`, `db_readonly`) |
| **Password** | 🔴 **บังคับ** | Password | รหัสผ่านที่ใช้เข้าถึงระบบ (จะถูกซ่อนหลังบันทึก) |
| **Purpose/Note**| ⚪ ทางเลือก | Text | (ในอนาคต) บันทึกย่อจุดประสงค์ของบัญชี |

---

## 4. ข้อมูลเสริมแบบยืดหยุ่น (Extended Metadata - สเปคฮาร์ดแวร์)
นี่คือฟีเจอร์ใหม่ที่ **มาแทนที่คอลัมน์ใน Excel เดิม** คุณไม่ต้องสร้างคอลัมน์ตายตัวอีกต่อไป แต่สามารถกรอก **Key (ชื่อหัวข้อ) และ Value (ค่าข้อมูล)** อะไรก็ได้ตามประเภทของ Asset

คุณสามารถเพิ่ม Field เหล่านี้ได้อย่างอิสระ หรือกำหนดมาตรฐานสำหรับทีมได้ตามตัวอย่าง:

### 🔹 ตัวอย่าง Metadata สำหรับเซิร์ฟเวอร์ปกติ (Physical/VM)
*   **Brand / Model:** `Dell PowerEdge R750`
*   **CPU Summary:** `2x Intel Xeon Gold 6330` หรือ `vCores: 16`
*   **RAM (GB):** `512` หรือ `32`
*   **Storage Info:** `1TB NVMe / 500GB HDD`
*   **Serial Number:** `DELL-SV-10023`
*   **Hardware Warranty Expire:** `2028-12-31`
*   **Backup Schedule:** `Daily 02:00 AM`

### 🔹 ตัวอย่าง Metadata สำหรับระบบเครือข่าย (Network / Firewall)
*   **Firmware / PanOS:** `NXOS 9.3` หรือ `10.1.8`
*   **HA Status:** `Active / Standby`
*   **Stack Role:** `Master / Member`

### 🔹 ตัวอย่าง Metadata สำหรับ Software/App/DB
*   **Database Engine:** `PostgreSQL 16` หรือ `MS SQL 2019`
*   **Application Framework:** `Next.js` หรือ `Spring Boot`
*   **Web URL:** `https://hr.infrapilot.local`
*   **Criticality:** `Tier 1 (Mission Critical)`

---

## 5. ข้อมูลบุคลากรและการเงิน (Ownership & Financial) - ทางเลือก
| หัวข้อ (Field) | ความจำเป็น | ประเภทข้อมูล | คำอธิบาย & ตัวอย่าง |
| :--- | :---: | :--- | :--- |
| **Owner** | ⚪ ทางเลือก | Text | ผู้รับผิดชอบหลัก (เช่น `IT Infrastructure Team`, `App Dev Team`, หรือชื่อบุคคล) |
| **Department** | ⚪ ทางเลือก | Text | แผนกที่ดูแล (เช่น `IT`, `HR`, `Finance`) |
| **Vendor** | ⚪ ทางเลือก | Text | บริษัทคู่สัญญาหรือผู้ดูแล (เช่น `SIS`, `Metro Systems`) |
| **Dependencies**| ⚪ ทางเลือก | Text | รายชื่อระบบที่ต้องพึ่งพา |

---

### 💡 คำแนะนำสำหรับการย้ายข้อมูลจาก Excel เดิม
หากคุณกำลังจะรวบรวมข้อมูลจากแผ่น Excel หลายๆ Sheet:
1.  **แยกประเภท (Asset Type) ให้ชัดเจนก่อน:** ว่าก้อนไหนคือเครื่องจริง (Server), ก้อนไหนคือ VM, ก้อนไหนคือแค่ Application
2.  **ดึงข้อมูลหลัก (Core Info) ออกมา:** ชื่อระบบ (Name), ไอพี (IP), ระบบปฏิบัติการ (OS Version), สภาพแวดล้อม (Environment) และ รหัสผ่าน
3.  **ส่วนที่เหลือ (RAM, สเปค, วันหมดประกัน, รุ่นเครื่อง):** ให้เตรียมจับโยนเข้าในส่วนของ **"Extended Metadata"** ตอนเพิ่มข้อมูล (หรือเตรียมเป็น JSON หากจะเขียนสคริปต์ Import อัตโนมัติในอนาคต) ได้เลย ไม่ต้องกังวลว่าฟิลด์ใน Database จะไม่พอครับ
